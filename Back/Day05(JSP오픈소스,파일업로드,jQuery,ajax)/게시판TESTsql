-- 자동번호 생성기
create sequence seq_board;

create table board(
    bno number primary key,
    title varchar2(100),
    content varchar2(2000),
    writer varchar2(30),
    pic1 varchar2(100),
    pic2 varchar2(100)
);
commit;
select * from board;