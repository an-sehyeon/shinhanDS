Day03(25.4.3)

1. 복습 (LAB2.java, day02pack(LAB2) )
    - 조건문, 반복문, LAB2 문제 풀이

2. 참조 타입 (ReferenceTest.java)
    - Data type
        1. 기본형 : 8가지
            - 기본타입으로 선언된 변수는 값 자체를 저장
            - 메모리에 저장

        2. 참조형 : 기본형 제외 전부
            - 객체의 번지를 참조하는 타입
            - 참조타입으로 선언된 변수는 객체가 생성된 메모리 번지를 저장
            - 메모리 영역의 힙(Heap) 영역에 저장

        - chartAt(): 문자열 추출, 
        - replace() : 기존 문자열은 그대로 두고 대체한 새로운 문자열로 대체
        - .toUpperCase() : 대문자로 변경
        - .equals() : 문자열 비교
        - .substring() : 문자열 잘라내기
        - .indexOf() : 문자열에서 특정 문자 위치 확인
        - .length : 문자열 길이 
        - .split() : 문자열 분리

3. 배열 (ArrayTest.java)
    - 배열은 힙 영역에 생성되고 배열 변수는 힙 영역의 배열 주소를 저장
    - 배열 : 같은 타입, 값 여러개, 연속공간, 접근은 첨자 이용
        1. 배열 참조 변수 선언
        2. 배열 생성(생성하지 않으면 사용불가), 반드시 배열의 갯수 지정
        3. 배열 사용

4. 다차원 배열(ArrayTest2.java)
    - 배열 항목안에 또 다른 배열이 대입된 배열
    - 2차원 배열 : 배열의 배열
        1. 배열 참조 변수 선언
        2. 배열 생성
        3. 배열 사용

    1) 배열 복사(f6())
        - System.arraycopy()

    2) 5장 LAB(1차,2차배열) 풀이 (f_sort()), (sub(), prn())


5. 열거 타입 (Enum.java, JobResult.java, Week.java)
    - 관련있는 상수들을 묶기 위함
    - 한정적인 값들의 묶음

